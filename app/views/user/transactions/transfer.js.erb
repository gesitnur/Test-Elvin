$('#transaction-form').html(`<%= render partial: 'user/transactions/partials/transfer_form',
                          locals: { recurring_transfers: @recurring_transfers, expenses: @expenses, incomes: @incomes, url: save_transfer_user_transactions_path } %>`);

$('.select-type').val("<%= params[:type] %>");

$.ajax({
  url: $('.select-type').data('url'),
  type: "GET",
  dataType:"script",
  data: {
    type: $('.select-type').val(),
    category: $('.category-id-field').val()
  },
})

format_number_field()
