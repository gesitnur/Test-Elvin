$('.leave-type-modal').html(`<%= render partial: 'user/leave_types/partials/leave_type_modal',
                            locals: { leave_type: @leave_type, url: user_leave_type_path(@leave_type) } %>`)

$('#leaveTypeLink').fadeIn(200)

$('.hideModal').click(function(){
  $(this).parents('.modal').fadeOut(100)
})

// $('.copy-link').click(function(){
//   Clipboard($(this).data('link'), {
//     debug: false,
//   });
//   var popup = document.getElementById("copiedPopup");
//   popup.classList.toggle("show");
// })


