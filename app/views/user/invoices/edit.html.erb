<div class="main-content-header mb-3">
  <h1 class="main-content-title text-3xl font-bold mb-5">Edit Invoice</h1>
</div>
<div class="main-content-card p-7 bg-white shadow-md rounded">
  <%= render partial: 'user/invoices/partials/form',
              locals: {
                invoice: @invoice,
                url: user_invoice_path(@invoice),
                text_submit: 'Save',
                services: @services,
                customers: @customers,
                back_url: user_invoice_path(@invoice)
              } %>
</div>
